{
  "name": "@drone/monorepo",
  "version": "0.1.0-beta.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/**/*",
    "tools/*"
  ],
  "scripts": {
    "context": "bun run --filter @cfd/scripts context",
    "facts:all": "bun run --elide-lines 0 --env-file ./.env --filter @cfd/fact-extractor all"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {},
  "trustedDependencies": [
    "kuzu"
  ],
  "dependencies": {
    "fastmcp": "^3.17.0"
  }
}

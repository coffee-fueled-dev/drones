{
  "name": "@drone/monorepo",
  "version": "0.1.0-beta.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/**/*",
    "tools/*"
  ],
  "scripts": {
    "context": "bun run --filter @cfd/scripts context",
    "facts:extract": "ENABLE_GRAPHITI=false bun run --env-file ./.env --filter @cfd/fact-extractor extract-facts",
    "facts:write": "ENABLE_GRAPHITI=true bun run --env-file ./.env --filter @cfd/fact-extractor write-facts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {},
  "trustedDependencies": [],
  "dependencies": {}
}
